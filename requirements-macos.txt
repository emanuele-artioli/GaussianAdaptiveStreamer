# macOS / Apple Silicon setup (no CUDA).
#
# This file intentionally avoids CUDA-only wheels and NVIDIA packages.
# The app can run in preview fallback mode when gsplat/CUDA rasterization
# is not available.

aioquic==1.2.0
flask==3.1.3
starlette==0.47.2

numpy==2.0.1
scipy==1.17.1
imageio==2.37.3
matplotlib==3.10.8
scikit-image==0.26.0
opencv-python==4.13.0.92
pillow==12.0.0
plyfile==1.1.3
tqdm==4.67.3
ninja==1.13.0

torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

basicsr==1.4.2
facexlib==0.3.0
gfpgan==1.3.8

# Optional on macOS: gsplat rasterization is typically CUDA-backed.
# Install manually if/when a compatible non-CUDA build is available.
# gsplat==1.5.3
